# Docker Compose / server defaults
APP_PORT=5001
SECRET_KEY=replace-with-a-strong-secret

# PostgreSQL
POSTGRES_DB=invoice_db
POSTGRES_USER=invoice_user
POSTGRES_PASSWORD=invoice_password
POSTGRES_PORT=5432

# DB connection pool
DB_POOL_MIN=2
DB_POOL_MAX=20
