# Local development
APP_PORT=5000
SECRET_KEY=dev-secret-key

# Keep local development on SQLite
DATABASE_URL=sqlite:///data/invoices.db

# Pool settings are ignored by SQLite, but keeping them here
# makes local/server env files easier to compare.
DB_POOL_MIN=2
DB_POOL_MAX=20
